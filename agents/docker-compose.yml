services:
  agent-server:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - agent-workdir:/app/workdir
    tmpfs:
      - /tmp:size=2G
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "2.0"

volumes:
  agent-workdir:
