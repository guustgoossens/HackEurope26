services:
  agent-server:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    tmpfs:
      - /tmp:size=2G
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "2.0"
